import {NextResponse} from 'next/server';
export async function POST(req:Request){const body=await req.json();if(!process.env.META_WHATSAPP_TOKEN||!process.env.META_WHATSAPP_PHONE_NUMBER_ID)return NextResponse.json({error:'Meta WhatsApp Cloud API is not configured'},{status:503});const r=await fetch(`https://graph.facebook.com/v23.0/${process.env.META_WHATSAPP_PHONE_NUMBER_ID}/messages`,{method:'POST',headers:{Authorization:`Bearer ${process.env.META_WHATSAPP_TOKEN}`,'Content-Type':'application/json'},body:JSON.stringify({messaging_product:'whatsapp',to:body.to,type:'text',text:{body:body.message}})});return NextResponse.json(await r.json(),{status:r.ok?200:r.status})}
